<%
=begin%> <!--<%= form_tag import_users_messages_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Import CSV", class: 'btn btn-outline-primary' %>
<% end %>
<%= link_to "/messages/users.csv", class: 'btn btn-outline-primary' do %>
  Export users csv
<% end %>
<%
=end
%>

<section class="better-space"></section>
<% if inbox_trips.present? %>
  <div class="container">
  <div class="messaging">
        <div class="inbox_msg">
          <div class="inbox_people">
            <div class="inbox_chat">
              <% inbox_trips.each do |trip| %>
                <div class="chat_list">
                <!--- add active_chat class to make it active ---> 
                  <div class="chat_people" data-trip-id="<%= trip.id %>" data-user-id="<%= current_user.id %>">
                    <div class="chat_ib">
                      <h5><%= "#{trip.start_location&.split(',')[0]} - #{trip.end_location&.split(',')[0]}" %> <span class="chat_date"><%= trip.messages.first.created_at.strftime('%d %B') %></span></h5>
                      <p><%= trip.messages.first.body %></p>
                    </div>
                  </div>
                </div> 
              <% end %>
            </div>
          </div>
          <div class="mesgs">
            <div class="msg_history">
              <% inbox_trips.first.messages.each do |message| %>
                  <% if message.user == current_user %>
                    <div class="outgoing_msg">
                      <div class="sent_msg">
                        <p><%= message.body %></p>
                        <span class="time_date"> <%= message.created_at.strftime('%H:%M %p') %> | <%= message.created_at.strftime('%d %B') %> </span> 
                      </div>
                      </div>
                  <% else %>
                    <div class="incoming_msg">
                      <h5><%= message.user.full_name %></h5>
                      <div class="received_msg">
                        <div class="received_withd_msg">
                          <p><%= message.body %></p>
                          <span class="time_date"><%= message.created_at.strftime('%H:%M %p') %> | <%= message.created_at.strftime('%d %B') %></span>
                        </div>
                      </div>
                    </div>
                  <% end %>
              <% end %>
            </div>
            <div class="type_msg">
              <div class="input_msg_write">
                <input type="text" class="write_msg" placeholder="Type a message" id="body" />
                <button class="msg_send_btn trip-message-sending" type="button" data-trip-id="<%= inbox_trips.first.id %>" data-user-id="<%= current_user.id %>"><i class="fa fa-paper-plane-o" aria-hidden="true"></i></button>
              </div>
            </div>
          </div>
        </div>      
  </div></div>
<% else %>
<main class="page">
    
    <!-- Title -->
    <h1 class="page-title">No messages in your inbox</h1>
    
    <!-- Content -->
    <div class="page-content">
        
        <!-- Placeholder Content -->
        <div class="placeholder-content">
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
            <div class="placeholder-content_item"></div>
        </div>
        
    </div>
    
</main>
<% end %>
